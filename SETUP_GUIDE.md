# 🏠 DLD Unit Finder — دليل التركيب الكامل

## ✅ الجديد: بدون Google Cloud وبدون Google Drive!

كل شي صار على GitHub بس. أبسط بكتير.

---

## 📋 شو بتحتاج

- حساب GitHub: https://github.com/signup
- حساب Streamlit (مجاني): https://share.streamlit.io
- بس! ما بتحتاج Google Cloud أبداً.

---

## ════════════════════════════════════════════
## الخطوة 1: حدّث الملفات على GitHub
## ════════════════════════════════════════════

لأنك عندك الـ repo جاهز (NABILNET-ORG/dld-unit-finder)، بس بدنا نحدّث 4 ملفات:

### الملفات يلّي بدها تتحدّث:
1. `convert_csv_to_db.py` — حذف كل كود Google Drive
2. `app.py` — بينزّل من GitHub Releases بدل Google Drive
3. `.github/workflows/update_data.yml` — بيرفع على GitHub Releases
4. `requirements.txt` — حذف Google dependencies

### كيف تحدّثهم:

**لكل ملف:**
1. روح على الـ repo: https://github.com/NABILNET-ORG/dld-unit-finder
2. اضغط على الملف (مثلاً `convert_csv_to_db.py`)
3. اضغط على أيقونة القلم ✏️ (Edit)
4. **امسح كل المحتوى** (Ctrl+A → Delete)
5. الصق المحتوى الجديد (من الـ ZIP يلّي رح أعطيك ياه)
6. اضغط **Commit changes** → **Commit changes**
7. كرّر لباقي الملفات

---

## ════════════════════════════════════════════
## الخطوة 2: شغّل GitHub Actions
## ════════════════════════════════════════════

1. روح على الـ repo → تاب **Actions**
2. اختار **Weekly DLD Data Update** (يسار)
3. اضغط **Run workflow** → **Run workflow** (الأخضر)
4. انتظر **15-25 دقيقة**:
   - بينزّل CSV من Dubai Pulse (~830MB)
   - بيحوّلها SQLite (2.3M+ صف)
   - بيضغطها بـ gzip (~200MB)
   - بيرفعها كـ GitHub Release تلقائياً
5. لما يخلص ✅ → روح على تاب **Code** → يمين بتشوف **Releases**
6. بتشوف "DLD Database" مع `dld_units.db.gz`

**ما بتحتاج تعمل شي تاني! كل أسبوع بيتحدث لحاله.**

---

## ════════════════════════════════════════════
## الخطوة 3: أنشئ GitHub Token (للـ repo الخاص)
## ════════════════════════════════════════════

لأنو الـ repo خاص (private)، الـ Streamlit app بيحتاج token لينزّل الداتابيس.

1. افتح: https://github.com/settings/tokens?type=beta
   (أو: Settings → Developer settings → Personal access tokens → Fine-grained tokens)
2. اضغط **Generate new token**
3. **Token name**: `streamlit-dld`
4. **Expiration**: اختار مدة طويلة (مثلاً 1 year) أو "No expiration"
5. **Repository access**: اختار **Only select repositories**
   → اختار `NABILNET-ORG/dld-unit-finder`
6. **Permissions** → Repository permissions:
   - **Contents**: ✅ **Read-only**
   - باقي الأشياء خلّيها "No access"
7. اضغط **Generate token**
8. **⚠️ مهم**: انسخ الـ token! ما رح يظهرلك مرة تانية.
   (شكله مثل: `github_pat_xxxxxxxxxxxxxxxxxxxx...`)
9. احفظه بمكان آمن

---

## ════════════════════════════════════════════
## الخطوة 4: Deploy على Streamlit Cloud
## ════════════════════════════════════════════

1. افتح: https://share.streamlit.io
2. سجّل بـ GitHub
3. **Create app**
4. عبّي:
   - **Repository**: `NABILNET-ORG/dld-unit-finder`
   - **Branch**: `main`
   - **Main file**: `app.py`
5. اضغط **Advanced settings**
6. بخانة **Secrets** اكتب:

   ```toml
   GITHUB_REPO = "NABILNET-ORG/dld-unit-finder"
   GITHUB_TOKEN = "github_pat_XXXXX_الصق_التوكن_هون"
   ```

7. اضغط **Save** → **Deploy!**
8. انتظر 3-5 دقائق
9. 🎉 **خلص!** صار عندك رابط يشتغل من أي مكان

---

## ════════════════════════════════════════════
## الخطوة 5: جرّب!
## ════════════════════════════════════════════

1. افتح الرابط (بيشتغل من موبايل كمان)
2. حطّ لينك Property Finder
3. اضغط 🔍
4. بيطلعلك unit number + كل الداتا

**كبسة Update Now**: بالـ sidebar بتنزّل آخر نسخة فوراً.

---

## ════════════════════════════════════════════
## ملخص الـ Secrets
## ════════════════════════════════════════════

| Secret | وين بيروح | شو هو |
|--------|-----------|-------|
| `GITHUB_REPO` | Streamlit Secrets | `NABILNET-ORG/dld-unit-finder` |
| `GITHUB_TOKEN` | Streamlit Secrets | Fine-grained token من GitHub |

**بس! ولا secret على GitHub.** الـ Actions بيستخدم `GITHUB_TOKEN` الـ built-in تلقائياً.

---

## ════════════════════════════════════════════
## بديل: خلّي الـ Repo عام (Public)
## ════════════════════════════════════════════

إذا بدك أبسط من هيك:
1. روح على الـ repo → Settings → Danger Zone → **Change visibility** → Public
2. هيك ما بتحتاج GitHub Token أبداً
3. بالـ Streamlit secrets بتحتاج بس:
   ```toml
   GITHUB_REPO = "NABILNET-ORG/dld-unit-finder"
   ```
4. الداتا هي open data من الحكومة فما في مشكلة تكون public

---

## مشاكل شائعة

### ❌ GitHub Action فشل
- تأكد إنو الملفات الجديدة مرفوعة صح
- Actions → اضغط على الـ run الفاشل → شوف الـ logs وين الخطأ

### ❌ Streamlit ما عم ينزّل الداتا
- تأكد إنو الـ token صحيح ومنسوخ كامل
- تأكد إنو الـ Release موجود (Code → Releases)
- جرّب **🔄 Update Now** بالـ sidebar

### ❌ ما عم يلاقي نتائج
- العقار ممكن off-plan ومش مسجّل بعد بالـ DLD
- جرّب لينك تاني من نفس المشروع
